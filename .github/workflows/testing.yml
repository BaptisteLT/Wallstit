name: CI

on:
  push:
    branches:
      - 'main'

jobs:
  phpunit:
    runs-on: ubuntu-20.04

    steps:
      - uses: actions/checkout@v3
      - uses: php-actions/composer@v6
      - run: vendor/bin/phpunit